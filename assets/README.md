# Assets

Put these files here:

- `dmg-background.png` — recommended 1600×800 PNG for the DMG window background
- `app-icon.icns` — your macOS app icon

The build script uses them automatically when present.
